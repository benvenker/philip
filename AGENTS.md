# Philip Agent Instructions

## Agent skills

### Issue tracker

Plans and PRDs may live as local Markdown; implementation task graphs live in Beads under `.beads/`, managed with `br`/`bv`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default Matt Pocock triage role labels unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: read root `CONTEXT.md` and relevant future ADRs under `docs/adr/`. See `docs/agents/domain.md`.
